# prod/dev profile choose hoga ENV se; local pe default dev
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}


spring.profile.active=prod
# Railway apko PORT env deta hai (502 avoid)
server.port=${PORT:8080}
